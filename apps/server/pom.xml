<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.sqlcopilot</groupId>
    <artifactId>sql-copilot-server</artifactId>
    <version>0.1.0</version>
    <name>sql-copilot-server</name>
    <description>SQL Copilot local backend service</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version>
        <relativePath/>
    </parent>

    <properties>
        <java.version>17</java.version>
        <mybatis.spring.boot.version>3.0.3</mybatis.spring.boot.version>
        <spring.ai.version>1.0.0-M6</spring.ai.version>
        <onnxruntime.version>1.19.2</onnxruntime.version>
        <onnxruntime.artifactId>onnxruntime</onnxruntime.artifactId>
        <djl.tokenizers.version>0.29.0</djl.tokenizers.version>
        <graalvm.buildtools.version>0.10.6</graalvm.buildtools.version>
        <sqlcopilot.packaging.variant>medium</sqlcopilot.packaging.variant>
        <sqlcopilot.rag.local-onnx-enabled>true</sqlcopilot.rag.local-onnx-enabled>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>${mybatis.spring.boot.version}</version>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.46.0.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-core</artifactId>
            <version>${spring.ai.version}</version>
        </dependency>
        <dependency>
            <groupId>com.microsoft.onnxruntime</groupId>
            <artifactId>${onnxruntime.artifactId}</artifactId>
            <version>${onnxruntime.version}</version>
        </dependency>
        <dependency>
            <groupId>ai.djl.huggingface</groupId>
            <artifactId>tokenizers</artifactId>
            <version>${djl.tokenizers.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.jsqlparser</groupId>
            <artifactId>jsqlparser</artifactId>
            <version>4.9</version>
        </dependency>
        <dependency>
            <groupId>com.github.mwiede</groupId>
            <artifactId>jsch</artifactId>
            <version>0.2.20</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
        </repository>
    </repositories>
    <profiles>
        <profile>
            <id>pack-minimal</id>
            <properties>
                <sqlcopilot.packaging.variant>minimal</sqlcopilot.packaging.variant>
                <sqlcopilot.rag.local-onnx-enabled>false</sqlcopilot.rag.local-onnx-enabled>
                <onnxruntime.artifactId>onnxruntime</onnxruntime.artifactId>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <configuration>
                            <excludes>
                                <exclude>
                                    <groupId>com.microsoft.onnxruntime</groupId>
                                    <artifactId>${onnxruntime.artifactId}</artifactId>
                                </exclude>
                            </excludes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>pack-medium</id>
            <properties>
                <sqlcopilot.packaging.variant>medium</sqlcopilot.packaging.variant>
                <sqlcopilot.rag.local-onnx-enabled>true</sqlcopilot.rag.local-onnx-enabled>
            </properties>
        </profile>
        <profile>
            <id>pack-full</id>
            <properties>
                <sqlcopilot.packaging.variant>full</sqlcopilot.packaging.variant>
                <sqlcopilot.rag.local-onnx-enabled>true</sqlcopilot.rag.local-onnx-enabled>
            </properties>
        </profile>
        <profile>
            <id>onnxruntime-gpu-windows</id>
            <activation>
                <os>
                    <family>Windows</family>
                </os>
            </activation>
            <properties>
                <onnxruntime.artifactId>onnxruntime_gpu</onnxruntime.artifactId>
            </properties>
        </profile>
        <profile>
            <id>onnxruntime-gpu-linux</id>
            <activation>
                <os>
                    <family>Linux</family>
                </os>
            </activation>
            <properties>
                <onnxruntime.artifactId>onnxruntime_gpu</onnxruntime.artifactId>
            </properties>
        </profile>
        <profile>
            <id>native</id>
            <properties>
                <spring.aot.enabled>true</spring.aot.enabled>
            </properties>
        </profile>
    </profiles>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.graalvm.buildtools</groupId>
                <artifactId>native-maven-plugin</artifactId>
                <version>${graalvm.buildtools.version}</version>
                <configuration>
                    <buildArgs>
                        <arg>--no-fallback</arg>
                        <arg>-H:+AllowDeprecatedBuilderClassesOnImageClasspath</arg>
                        <arg>-H:+ReportExceptionStackTraces</arg>
                    </buildArgs>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
