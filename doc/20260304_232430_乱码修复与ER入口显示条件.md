# 乱码修复与 ER 入口显示条件（2026-03-04 23:24）

## 本次目标
1. 修复系统中的乱码问题（前端核心页面）。
2. 调整“智能ER图”按钮显示时机：仅在对象列表类型为表时显示。

## 代码改动
### 1) App.vue 乱码修复
- 清理了 `apps/desktop/src/App.vue` 中残留的异常字符（`�`）注释与文案。
- 确认与“建表 SQL 展示”相关文案已为正常中文（如“当前未选中表”“未读取到表 ... 的字段元数据”）。
- 全仓扫描 `�`：已无匹配。

### 2) 智能 ER 图按钮显示条件
- 文件：`apps/desktop/src/App.vue`
- 位置：对象浏览工具栏左侧按钮容器。
- 调整：增加 `v-if="currentObjectType === 'tables'"`，使“智能ER图”仅在当前对象类型为表时显示。

### 3) 编码基线约束
- 新增 `.editorconfig`，统一声明：
  - `charset = utf-8`
  - `end_of_line = lf`
  - `insert_final_newline = true`
- 目的：减少后续编辑产生乱码的概率。

## 验证结果
1. 前端静态检查
- `npm run -w @sqlcopilot/desktop type-check`：通过。

2. 前端构建
- `npm run -w @sqlcopilot/desktop build`：通过。

3. 后端 clean + 启动验证
- `mvn clean package`：通过。
- `java -jar target/sql-copilot-server-0.1.0.jar --server.port=19124`：日志包含 `Started SqlCopilotApplication`，验证后停止。

4. 前端 preview 启动验证
- `npm run -w @sqlcopilot/desktop preview`：成功监听 `4173`，验证后停止进程。
