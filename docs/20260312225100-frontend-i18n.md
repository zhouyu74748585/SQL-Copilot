# 主题：frontend-i18n

## 记录

### 2026-03-12 22:51:00

## 本次目标
- 为桌面端前端增加中英文国际化能力。
- 在尽量少改动现有大体量中文文案的前提下，先完成可切换、可运行、可验证的双语方案。

## 关键改动
- 前端 `apps/desktop/src/i18n/messages.ts`
  - 新增前端国际化词典与规则翻译，覆盖主界面、常用提示、弹窗按钮、动态标题等高频文案。
  - 支持中英文双向转换，并对带变量的动态文本使用正则规则做运行时翻译。
- 前端 `apps/desktop/src/i18n/index.ts`
  - 新增全局语言状态、`localStorage` 持久化、`document.lang` 同步。
  - 接入 Ant Design Vue 的 `zh_CN` / `en_US` locale。
  - 对 `message`、`Modal` 做运行时封装，自动翻译通知和弹窗文案。
  - 新增 DOM 观察式翻译层，对现有模板里的静态中文文本、`title`、`placeholder` 等属性做无侵入切换。
- 前端 `apps/desktop/src/modules/studio/components/StudioShell.vue`
  - 在顶部操作区新增语言切换下拉框。
  - 将 `a-config-provider` 绑定到当前语言 locale。
  - 在工作台根组件启用 DOM 国际化同步。
- 前端 `apps/desktop/src/main.ts`
  - 应用启动时初始化国际化运行时补丁。
- 样式 `apps/desktop/src/modules/studio/styles/shell.css`
  - 为顶部语言切换器补充样式，保持与现有导航栏视觉一致。

## 方案说明
- 当前工程前端中文文案分布广、历史代码多，若一次性全部改造成 key-based i18n，改动范围会显著扩大。
- 本次先采用“全局语言状态 + Ant Design locale + 运行时翻译层”的过渡方案，优先满足中英文切换能力和界面可用性。
- 后续如继续演进，可逐步把核心模块文案迁移到标准 key/message 结构，最终替换掉 DOM 运行时翻译层。

## 验证结果
- 前端类型检查：`npm run -w @sqlcopilot/desktop type-check` 通过。
- 前端 clean 构建：`npm run -w @sqlcopilot/desktop build -- --emptyOutDir` 通过。
- 后端 clean 打包并启动：
  - `mvn.cmd -f apps/server/pom.xml clean package '-DskipTests' '-Dfile.encoding=UTF-8'` 通过。
  - `java -Dfile.encoding=UTF-8 -jar apps/server/target/sql-copilot-server-0.1.0.jar --server.port=18110` 启动成功。
  - `http://127.0.0.1:18110/api/health` 返回 `{"code":0,"message":"success","data":"ok"}`。
- 前端预览：
  - `npx vite preview --host 127.0.0.1 --port 6060 --strictPort` 启动成功。
  - `http://127.0.0.1:6060` 返回 `HTTP 200`。

## 当前边界
- 本次优先覆盖高频界面与运行时提示，未把所有前端文案完全重写为显式 key。
- 对极少数未进入词典或规则的长尾文案，英文模式下仍可能保持原语言，后续可按模块继续补齐。

### 2026-03-13 07:19:44

## 本次目标
- 将语言设置入口从顶栏迁移到设置弹窗内部。
- 补齐查询页、ER 图页、对象详情等子页面的国际化覆盖，尤其是 hover、tooltip、动态提示和部分图表/ER 组件内部文案。
- 修复暗色模式下对象详情和 ER 图界面的样式未正确切换问题。

## 关键改动
- 前端 `apps/desktop/src/modules/studio/components/StudioShell.vue`
  - 移除顶栏语言切换下拉框。
  - 将原 AI 配置弹窗提升为“设置”弹窗，新增“界面设置”页签，把语言切换和深色模式开关放入设置内部。
  - 顶栏设置按钮文案从“配置”调整为“设置”。
- 前端 `apps/desktop/src/modules/studio/composables/useStudioRuntime.ts`
  - 将设置页签状态扩展为 `general | model | embedding`，支持新的“界面设置”页签。
- 前端 `apps/desktop/src/i18n/messages.ts`
  - 扩充 query、ER、对象详情、设置弹窗相关词典。
  - 新增动态规则翻译，覆盖“模型 N”“连接：xxx”“数据库：xxx”“共 N 行”“执行成功 · 耗时 Nms · 影响行数 M”等文本。
- 前端 `apps/desktop/src/components/ErDiagramPanel.vue`
  - 增加国际化辅助方法，覆盖字段展开/收起、连线 hover、删除菜单、ER tooltip、自定义 tooltip 文本和画布内附加文案。
  - 增加暗色模式 class，并为 ER 画布、卡片、tooltip、右键菜单、字段标记等补充 dark 主题样式。
- 前端 `apps/desktop/src/components/QueryChartPanel.vue`
  - 图表空态文案和默认系列名改为跟随当前语言实时切换。
- 样式 `apps/desktop/src/modules/studio/styles/shell.css`
  - 去除顶栏语言切换样式。
  - 为对象详情、知识详情、ER 侧栏、ER 关系块等区域补充主题变量驱动的亮/暗样式。

## 验证结果
- 前端类型检查：`npm run -w @sqlcopilot/desktop type-check` 通过。
- 前端 clean 构建：`npm run -w @sqlcopilot/desktop build -- --emptyOutDir` 通过。
- 后端 clean 打包并启动：
  - `mvn.cmd -f apps/server/pom.xml clean package '-DskipTests' '-Dfile.encoding=UTF-8'` 通过。
  - `java -Dfile.encoding=UTF-8 -jar apps/server/target/sql-copilot-server-0.1.0.jar --server.port=18111` 启动成功。
  - `http://127.0.0.1:18111/api/health` 返回 `{"code":0,"message":"success","data":"ok"}`。
- 前端预览：
  - `npx vite preview --host 127.0.0.1 --port 6061 --strictPort` 启动成功。
  - `http://127.0.0.1:6061` 返回 `HTTP 200`。

## 当前边界
- 本次已补齐一批 query / ER / 对象详情高频文案，但前端仍有零散长尾文案分布在其它组件与 composable 中，后续仍需继续收口。
- 当前国际化仍以运行时翻译层为主，后续若继续深入，可逐步把核心页面迁移为显式 key-based i18n。


### 2026-03-13 09:48:00

## 本次目标
- 修复英文模式下“新建连接”按钮显示为“New 链接”的国际化异常。

## 关键改动
- 前端 `apps/desktop/src/modules/studio/components/StudioShell.vue`
  - 将左侧连接区按钮文本从错误的“新建链接”修正为“新建连接”。
- 前端 `apps/desktop/src/i18n/messages.ts`
  - 增加“新建链接 -> New Connection”兼容映射，避免历史或残留文案再次出现半中半英显示。

## 验证结果
- 前端类型检查：`npm run -w @sqlcopilot/desktop type-check` 通过。
- 前端 clean 构建：`npm run -w @sqlcopilot/desktop build -- --emptyOutDir` 通过。
- 后端 clean 打包并启动：
  - `mvn -f apps/server/pom.xml clean package -DskipTests -Dfile.encoding=UTF-8` 通过。
  - `java -Dfile.encoding=UTF-8 -jar apps/server/target/sql-copilot-server-0.1.0.jar --server.port=18112` 启动成功。
  - `http://127.0.0.1:18112/api/health` 返回 `{"code":0,"message":"success","data":"ok"}`。
- 前端预览：
  - `npx vite preview --host 127.0.0.1 --port 6062 --strictPort` 启动成功。
  - `http://127.0.0.1:6062` 返回 `HTTP 200`。

### 2026-03-20 08:51:47

## 本次目标
- 复查桌面端各页面的中英国际化覆盖，补齐近期回归后遗漏的静态文案。
- 复查暗色模式在表结构编辑、知识中心、设置等高频页面的样式完整性。
- 在真实运行态下完成对象浏览、AI 查询、表数据、表结构、对象定义、ER、历史、知识、Redis、设置等 10 个模块的巡检。

## 关键改动
- 前端 `apps/desktop/src/components/TableEditor.vue`
  - 将表名、备注、字段/索引页签、字段明细、索引类型、预览空态等显式接入 `translateText`，避免继续依赖 DOM 翻译兜底。
  - 将表结构编辑器工具条、字段详情面板、选中态等样式改为使用主题变量，补齐暗色模式下的背景、边框与文字层级。
- 前端 `apps/desktop/src/modules/studio/components/StudioConnectionContextBar.vue`
  - 将连接/数据库标签改为运行时翻译后的响应式文案，确保语言切换后立即更新。
- 前端 `apps/desktop/src/modules/studio/components/TableDataVirtualGrid.vue`
  - 将空态文案改为显式国际化，避免数据为空时在英文模式保留中文。
- 前端 `apps/desktop/src/config/packageVariant.ts`
  - 将 RAG provider 下拉选项改为基于当前 locale 动态生成，修复“本地 ONNX / 在线 OpenAI 兼容”在英文设置页中的静态中文残留。
- 前端 `apps/desktop/src/modules/studio/composables/useStudioRuntime.ts`
  - 接入基于当前语言的 provider 选项构建逻辑。
- 前端 `apps/desktop/src/modules/studio/components/StudioShell.vue`
  - 新增 `tt()` 显式翻译辅助方法。
  - 补齐知识中心详情按钮、设置弹窗标题/按钮/页签/隐私政策按钮等显式文案翻译。
- 前端 `apps/desktop/src/i18n/messages.ts`
  - 新增一批设置、保存、创建、重置、隐私政策、筛选折叠、表复制等补充词条。
  - 新增 `结果 N · xxx`、`定义编辑 · xxx`、`xxx · 对话` 等动态规则，修复顶部页签和结果标题在英文模式残留中文的问题。
- 后端 `apps/server/src/main/java/com/sqlcopilot/studio/support/driver/IsolatedJdbcConnectionManager.java`
  - 将隔离 JDBC 驱动类加载器的父加载器从 `PlatformClassLoader` 调整为应用类加载器，修复 SQLite 演示连接在真实巡检时触发的 `NoClassDefFoundError: org/slf4j/LoggerFactory`。

## 巡检结果
- `zh-CN + light`
  - 已完成 10 个模块的基线巡检与最终截图采集。
- `en-US + light`
  - 已修复查询结果标题、知识页签标题等英文模式残留中文问题。
- `en-US + dark`
  - 已修复知识中心详情按钮、设置隐私政策入口、设置弹窗按钮等英文暗色模式残留中文问题。
  - 表结构编辑、知识中心、设置弹窗和查询页未发现明显浅色残块、弱对比边框或不可读文本。
- `zh-CN + dark`
  - 已做快速回归，主题切换未出现明显反向回退。

## 验证结果
- 前端类型检查：`npm run type-check` 通过。
- 前端 clean 构建：`npm run build` 通过。
- 后端 clean 打包：`mvn.cmd -f apps/server/pom.xml clean package '-DskipTests' '-Dfile.encoding=UTF-8'` 通过。
- 后端启动验证：
  - `java -Dfile.encoding=UTF-8 -jar apps/server/target/sql-copilot-server-0.1.0.jar` 已以 `SQLCOPILOT_DATA_DIR=D:\Ideaprojects\SQL-Copilot` 启动。
  - `http://127.0.0.1:18080/api/health` 返回 `{"code":0,"message":"success","data":"ok"}`。
- 前端预览验证：
  - `npm run -w @sqlcopilot/desktop preview -- --host 127.0.0.1 --port 6068 --strictPort` 启动成功。
  - `http://127.0.0.1:6068/` 返回 `HTTP 200`。

## 说明
- 本轮为完成截图和巡检，补充了本地 SQLite 演示数据与元数据库记录；这些数据位于工作区 `sql-copilot.db` 与 `target/codex-qa/sql-copilot-demo.sqlite`。
- README 最终截图统一使用中文浅色主题，英文与暗色模式截图仅作为 QA 证据保留在 `target/codex-qa/`。
